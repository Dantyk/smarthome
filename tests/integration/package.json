{
  "name": "smarthome-integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for SmartHome system",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:mqtt": "mocha mqtt-integration.test.js --timeout 10000",
    "test:api": "mocha api-contract.test.js --timeout 5000",
    "test:all": "npm run test && npm run test:mqtt && npm run test:api"
  },
  "keywords": ["testing", "integration", "e2e"],
  "author": "Dantyk",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "mqtt": "^5.3.0",
    "axios": "^1.6.0",
    "chai": "^4.3.10",
    "mocha": "^10.2.0"
  }
}
